# TODO

- Rename historical `flight_cores/`, `CoreBase`, and `run_core()` surfaces to Program terminology once runtime behavior is stable.
- Finish Raspberry Pi target/interface support.
- Expand endpoint coverage only when a demonstrated OCCID concept is not yet represented; use `deep_ontology` as semantic reference rather than restoring its full taxonomy.
- Add focused integration acceptance for direct-control sessions, asynchronous command result forwarding, and restored MSP state surfaces.
